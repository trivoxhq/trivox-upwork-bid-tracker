import DesignSystemDemo from "@/components/ui/design-system-demo";

/** Internal UI reference — not linked from production nav by default. */
export default function DesignPage() {
  return <DesignSystemDemo />;
}
