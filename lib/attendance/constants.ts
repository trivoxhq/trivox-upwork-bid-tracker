export const ATTENDANCE_SETTINGS_ID = "default";

export const DEFAULT_SHIFT_TOTAL_MINUTES = 540;
export const DEFAULT_BREAK_ALLOWANCE_MINUTES = 60;
export const DEFAULT_MIN_FULL_DAY_WORKING_MINUTES = 480;
export const DEFAULT_MIN_HALF_DAY_WORKING_MINUTES = 300;
export const DEFAULT_WORKING_DAYS_PER_MONTH = 22;

export const MIN_SUMMARY_LENGTH = 20;

export const WORK_TIMEZONE = "Asia/Karachi";

export type LeaveButtonMode = "locked" | "half_day" | "check_out";

export type AttendanceDayType = "half_day" | "full_day";
